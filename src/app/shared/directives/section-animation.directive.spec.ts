/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SectionAnimationDirective } from './section-animation.directive';

describe('Directive: SectionAnimation', () => {
  it('should create an instance', () => {
    const directive = new SectionAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
